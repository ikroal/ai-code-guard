# Standard preset - 团队项目（默认）
# format + lint + test + coverage + audit

code:
  commit:
    format: true
  push:
    lint: true
    checks:
      test:
        command: "pytest --cov"
        pass_filenames: false

output:
  audit:
    enabled: true
